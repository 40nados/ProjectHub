{
  "dependencies": {
    "concurrently": "^9.0.1"
  },
  "scripts": {
    "dev": "concurrently --kill-others-on-fail -n \"BACKEND,FRONTEND\" -c \"yellow,blue\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  }
}
