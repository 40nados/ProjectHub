import React from 'react';

const register = () => {
    return (
        <main className="bg-black min-h-screen">
            <h1 className="text-white">Regsiter</h1>
        </main>
    );
};

export default register;